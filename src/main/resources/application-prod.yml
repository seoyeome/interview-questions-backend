spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}?sslmode=require
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    placeholder-replacement: false
    
server:
  port: ${PORT:8080}
  servlet:
    context-path: /
    
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    
management:
  endpoints:
    web:
      exposure:
        include: [ "health", "info", "prometheus" ]
  endpoint:
    health:
      show-details: when_authorized
  prometheus:
    metrics:
      export:
        enabled: true
  otlp:
    metrics:
      export:
        url: ${GRAFANA_OTLP_ENDPOINT:https://otlp-gateway-prod-ap-northeast-0.grafana.net/otlp/v1/metrics}
        headers:
          Authorization: Basic ${GRAFANA_BASIC_AUTH}
  metrics:
    export:
      otlp:
        enabled: true
        step: 60s

logging:
  level:
    root: INFO
    com.example: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate: INFO

# OAuth2 리다이렉트 URI (Next.js API Route로 변경)
app:
  oauth2:
    redirect-uri: https://interview-questions-frontend-zeta.vercel.app/api/auth/callback
  cookie:
    secure: true  # HTTPS에서만 쿠키 전송
    same-site: "Lax"  # Same-Site 쿠키

sentry:
  dsn: ${SENTRY_DSN}
  environment: production
  traces-sample-rate: 1.0
  enable-tracing: true
