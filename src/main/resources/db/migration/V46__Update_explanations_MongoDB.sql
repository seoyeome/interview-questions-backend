-- MongoDB 질문에 대한 답변 추가

UPDATE questions
SET explanation =
'**개념**
- **MongoDB**는 JSON과 유사한 **문서(Document)** 형태로 데이터를 저장하는 대표적인 **문서 지향(Document-oriented) NoSQL DB**입니다.
- 전통적인 **SQL(RDBMS)**는 테이블/행/열과 정규화 중심인 반면, MongoDB는 **컬렉션(Collection)/문서(Document)** 중심이며 문서 내부에 중첩 구조(embedded document, array)를 자연스럽게 담습니다.

**핵심 차이**
1) **스키마**
- RDBMS: 스키마(컬럼/타입)가 엄격하고 변경 비용이 큼
- MongoDB: 기본적으로 스키마를 강제하지 않음(애플리케이션/검증 규칙으로 관리)

2) **조인**
- RDBMS: 조인이 핵심 기능
- MongoDB: 기본은 문서 중첩(embedding)과 참조(reference) 설계, 필요 시 `$lookup`로 조인 유사 처리 가능

3) **확장**
- RDBMS: 수직 확장(Scale-up) 중심 + 샤딩은 별도 설계/운영 부담
- MongoDB: 샤딩(Sharding) 기반 **수평 확장(Scale-out)**을 핵심 기능으로 제공

4) **트랜잭션**
- RDBMS: 전통적으로 강력한 ACID 트랜잭션
- MongoDB: 단일 문서 원자성은 기본, (현대 버전에서는) 복수 문서/컬렉션 트랜잭션도 지원하지만 비용이 있으므로 설계 선택이 중요합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000000';

UPDATE questions
SET explanation =
'**이유**
- **NoSQL**은 “관계형 모델 + SQL 중심”이 아닌 DB를 통칭하는 분류입니다.
- MongoDB는 테이블/조인/정규화 대신, **문서 모델**(BSON 문서)과 **유연한 스키마**, **분산/수평 확장**을 중심으로 설계되어 NoSQL로 분류됩니다.

**정리**
- NoSQL = “SQL을 절대 못 쓴다”가 아니라, **관계형 모델을 기본 전제로 하지 않는 데이터 모델/확장 전략**을 택한 DB라는 의미입니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000001';

UPDATE questions
SET explanation =
'**개념**
- **문서 지향 데이터베이스**는 데이터를 행/열이 아니라 **문서(Document)** 단위로 저장합니다.
- 문서는 보통 JSON과 유사한 구조(키-값, 중첩 객체, 배열)를 가지며, 하나의 문서가 하나의 “레코드” 역할을 합니다.

**장점**
1) 도메인 객체 구조와 유사해서 **직렬화/역직렬화**가 자연스럽다
2) 중첩 구조로 **연관 데이터를 한 문서에 담아** 읽기 성능을 높일 수 있다(embedding)
3) 스키마 유연성으로 빠른 변화에 대응이 쉽다

**주의**
- 문서가 과도하게 커지면 갱신/전송 비용이 커지고, 중복(denormalization)으로 인해 일관성 관리 비용이 증가할 수 있습니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000002';

UPDATE questions
SET explanation =
'**개념**
- MongoDB에서 **컬렉션(Collection)**은 문서(Document)들의 집합입니다.
- RDBMS 관점에서 비유하면 **테이블(table)**에 가깝지만, 컬렉션은 테이블보다 스키마 제약이 덜하고 문서별로 필드 구성이 달라질 수 있습니다.

**특징**
- 컬렉션 단위로 인덱스 생성, 권한, 샤딩 설정 등을 적용합니다.
- 같은 컬렉션 내 문서라도 필드가 일부 달라도 저장이 가능합니다(단, 일관된 설계를 권장).'
WHERE id = 'b0000000-0000-0000-0012-000000000003';

UPDATE questions
SET explanation =
'**BSON이란**
- **BSON(Binary JSON)**은 JSON을 이진(binary) 형태로 확장한 포맷입니다.
- MongoDB는 내부적으로 BSON을 사용해 문서를 저장/전송합니다.

**왜 BSON을 쓰나**
1) 숫자/날짜/바이너리/Decimal 등 **타입 표현이 풍부**
2) 이진 포맷이라 **파싱/전송 효율**이 좋음
3) 필드 길이 정보 등을 포함해 특정 작업에서 빠른 접근이 가능

**주의**
- “그냥 JSON”이 아니라 타입/크기/정렬 등 저장 관점에서 특성이 다르므로, 날짜/정밀도(Decimal) 같은 타입 선택을 신중히 해야 합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000004';

UPDATE questions
SET explanation =
'**의미**
- MongoDB를 “스키마리스(schema-less)”라고 부르는 것은 **DB가 컬렉션에 고정 스키마를 강제하지 않는다**는 의미입니다.
- 즉, 동일 컬렉션의 문서들이 서로 다른 필드를 가질 수 있습니다.

**중요한 오해 정리**
- “스키마가 없다”가 아니라 **스키마 강제가 DB에만 의존하지 않는다**는 뜻입니다.
- 실무에서는 보통 아래 방식으로 스키마를 관리합니다.
  1) 애플리케이션 레벨 DTO/도메인 모델로 규격 관리
  2) MongoDB **Schema Validation**(JSON Schema 기반)으로 최소 규칙 강제
  3) 마이그레이션 전략(버전 필드, 점진적 변환) 적용'
WHERE id = 'b0000000-0000-0000-0012-000000000005';

UPDATE questions
SET explanation =
'**장점**
1) **유연한 데이터 모델**: 중첩/배열 구조로 객체 모델을 자연스럽게 저장
2) **빠른 개발 속도**: 스키마 변경이 잦은 서비스에서 대응이 쉬움
3) **수평 확장 친화**: 샤딩/레플리카 구조를 기본 기능으로 제공
4) **읽기 최적화 설계**: embedding + 인덱스 설계로 특정 조회 패턴에 강함
5) **풍부한 쿼리/집계**: 필터링, 정렬, 집계 파이프라인 제공

**단점/주의**
1) **조인 중심 모델에 불리**: 다중 조인이 많으면 설계/성능이 어려울 수 있음
2) **중복(denormalization)로 인한 일관성 비용**: 동일 데이터가 여러 문서에 있으면 갱신 동기화가 필요
3) **트랜잭션 비용**: 멀티 문서 트랜잭션은 가능하지만 비용/복잡도가 증가
4) **스키마 유연성의 역효과**: 규칙 없이 쓰면 필드 난립/데이터 품질 저하
5) 인덱스/샤딩 키 선택이 잘못되면 성능/확장성 문제가 크게 발생'
WHERE id = 'b0000000-0000-0000-0012-000000000006';

UPDATE questions
SET explanation =
'**수평 확장 관점 이점**
- MongoDB는 **샤딩(Sharding)**을 통해 데이터를 여러 노드로 분산 저장하고, 트래픽을 분산 처리할 수 있습니다.

**핵심 구성요소(요지)**
1) **Shard**: 실제 데이터를 저장하는 노드(보통 Replica Set 구성)
2) **mongos**: 애플리케이션의 요청을 받아 적절한 샤드로 라우팅하는 라우터
3) **Config Server**: 메타데이터(샤드 범위/청크 등) 관리

**효과**
- 데이터가 커지고 요청이 증가할 때 단일 서버 증설(Scale-up) 대신 **노드 추가(Scale-out)**로 용량/처리량을 늘릴 수 있습니다.

**주의**
- 샤드 키가 나쁘면 특정 샤드에 쏠리는 **핫스팟**이 생겨 수평 확장이 제대로 안 됩니다(Shard Key 설계가 핵심).'
WHERE id = 'b0000000-0000-0000-0012-000000000007';

UPDATE questions
SET explanation =
'**인덱스 동작**
- MongoDB 인덱스는 특정 필드(또는 필드 조합)에 대한 **탐색 구조(B-Tree 계열)**를 만들어, 조건 검색/정렬을 빠르게 합니다.
- 쿼리 실행 시 조건과 정렬에 맞는 인덱스를 선택하면, 컬렉션 전체 스캔(COLLSCAN) 대신 인덱스 스캔(IXSCAN)을 통해 성능을 크게 개선합니다.

**효과**
1) `find` 필터 성능 향상
2) `sort` 성능 향상(정렬 인덱스 활용)
3) `unique` 제약(유니크 인덱스)로 중복 방지 가능
4) 집계 파이프라인에서도 초반 `$match` 단계에서 인덱스 활용 가능

**주의**
- 인덱스는 읽기를 빠르게 하지만, 쓰기 시 인덱스도 갱신해야 하므로 **쓰기 성능/저장 공간** 비용이 있습니다.
- 실무에서는 `explain()`로 실행 계획을 확인하고, 필요한 인덱스만 최소로 두는 것이 좋습니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000008';

UPDATE questions
SET explanation =
'**일관성 유지 방법(대표)**
1) **단일 문서 설계(embedding)**
- 한 문서 안의 변경은 기본적으로 원자적이므로(한 문서 단위), 관련 데이터를 한 문서로 묶으면 일관성 유지가 쉽습니다.

2) **트랜잭션(멀티 문서)**
- Replica Set/샤딩 환경에서도 트랜잭션을 지원합니다(조건/비용 고려).
- 여러 컬렉션/문서 업데이트가 동시에 일어나야 할 때 사용합니다.

3) **Write Concern / Read Concern 조절**
- 쓰기 확인 수준(예: majority)과 읽기 일관성 수준을 조절해 “얼마나 최신/안전한 데이터를 보장할지” 선택합니다.

4) **유니크 인덱스/검증 규칙**
- unique index로 중복을 방지하고, Schema Validation으로 필드 규칙을 강제해 데이터 품질을 유지합니다.

5) **애플리케이션 레벨 보상 로직**
- 중복 저장/비정상 상태가 생길 수 있는 구조라면, 재처리/보상/정합성 체크 배치 등을 함께 설계합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000009';

UPDATE questions
SET explanation =
'**중복을 허용할 때 생길 수 있는 문제**
1) **갱신 불일치**
- 동일 정보가 여러 문서에 중복되면, 일부만 업데이트되어 서로 다른 값이 남을 수 있습니다.

2) **읽기 결과의 모순**
- 화면/리포트에서 서로 다른 문서의 중복 필드를 조합할 때, 시점 차이로 일관성이 깨질 수 있습니다.

3) **저장 공간/인덱스 비용 증가**
- 중복 데이터는 저장 공간을 늘리고, 인덱스가 붙으면 더 큰 비용이 됩니다.

4) **수정 난이도 증가**
- “어디에 중복이 퍼져 있는지”를 추적해야 해서 마이그레이션/정정 작업이 어려워집니다.

**실무 기준**
- 중복은 “읽기 최적화”를 위해 의도적으로 쓰는 경우가 많습니다.
- 다만 중복을 쓰면, 반드시 **정합성 유지 전략(트랜잭션/이벤트 기반 동기화/배치 검증)**을 같이 설계해야 합니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000000A';

UPDATE questions
SET explanation =
'**개념**
- **복합 인덱스(Composite Index)**는 두 개 이상의 필드를 결합해 만든 인덱스입니다.
- 예: `{ userId: 1, createdAt: -1 }`

**언제 유용한가**
1) `userId = ? AND createdAt >= ?` 같은 다중 조건 검색
2) `userId`로 필터링 후 `createdAt` 정렬 같은 패턴
3) “항상 같이 쓰는” 조건/정렬 조합을 커버할 때

**주의(인덱스 순서)**
- 복합 인덱스는 **필드 순서가 매우 중요**합니다.
- 보통 “선택도가 높은 필터” → “정렬/범위” 순으로 설계하고,
- `prefix` 원칙(앞쪽 필드 조건이 있어야 뒤쪽을 제대로 활용)을 고려해야 합니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000000B';

UPDATE questions
SET explanation =
'**CRUD**
- Create: `insertOne`, `insertMany`
- Read: `find`, `findOne`
- Update: `updateOne`, `updateMany`, `replaceOne`
- Delete: `deleteOne`, `deleteMany`

**추가로 자주 쓰는 것**
- `bulkWrite`(대량 처리)
- `findOneAndUpdate`, `findOneAndDelete`(조회+변경을 한 번에)
- `upsert` 옵션(없으면 insert, 있으면 update)

**팁**
- 업데이트는 전체 문서를 갈아끼우는 `replaceOne`보다, 변경 필드만 수정하는 **업데이트 연산자**를 사용하는 편이 안전한 경우가 많습니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000000C';

UPDATE questions
SET explanation =
'**기본 사용법**
- Mongo Shell / 드라이버에서 컬렉션에 대해 `find(query, projection)` 형태로 조회합니다.

**예시**
1) 전체 조회
- `db.users.find({})`

2) 조건 조회
- `db.users.find({ status: "ACTIVE" })`

3) 특정 필드만
- `db.users.find({ status: "ACTIVE" }, { email: 1, name: 1, _id: 0 })`

4) 정렬/페이징
- `db.users.find({ status: "ACTIVE" }).sort({ createdAt: -1 }).skip(20).limit(10)`

**주의**
- 조건/정렬 패턴에 맞는 인덱스가 없으면 COLLSCAN이 발생하므로, `explain()`으로 확인하는 습관이 중요합니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000000D';

UPDATE questions
SET explanation =
'**업데이트 연산자 개념**
- MongoDB는 문서 전체를 교체하지 않고, 특정 필드만 수정하는 다양한 연산자를 제공합니다.

**대표 연산자**
1) `$set`
- 지정 필드를 설정/수정
- 예: `db.users.updateOne({ _id: 1 }, { $set: { name: "Kim" } })`

2) `$inc`
- 숫자 필드 증감
- 예: `db.counters.updateOne({ key: "seq" }, { $inc: { value: 1 } })`

3) `$unset`
- 필드 제거
- 예: `{ $unset: { tempField: "" } }`

4) 배열 연산
- `$push`, `$addToSet`, `$pull`, `$pop`, `$each` 등

**주의**
- 동시성 환경에서는 “읽고 계산 후 set”보다 `$inc` 같은 원자 연산자를 쓰는 편이 안전합니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000000E';

UPDATE questions
SET explanation =
'**Aggregation(집계)란**
- MongoDB의 **집계 프레임워크**는 데이터를 단계별 파이프라인으로 변환/필터/그룹화하여 통계/리포트를 만드는 기능입니다.
- SQL의 `GROUP BY`, `JOIN`, `SUM/AVG` 같은 분석 쿼리에 대응합니다.

**구성**
- `db.collection.aggregate([ { stage1 }, { stage2 }, ... ])`

**장점**
1) 단계별로 가독성 있게 처리(필터 → 조인/변환 → 그룹 → 정렬)
2) `$lookup`, `$unwind` 등으로 관계형 연산 일부를 수행 가능
3) 큰 데이터에서 인덱스 활용과 함께 성능 최적화 가능(초기 `$match` 중요)

**주의**
- 파이프라인이 과도하게 복잡해지면 유지보수가 어려워지므로, 목적에 따라 사전 집계/ETL/분리 설계를 고려합니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000000F';

UPDATE questions
SET explanation =
'**주요 단계**
1) `$match`
- 조건 필터(가능하면 파이프라인 앞쪽에 배치해 데이터량을 줄입니다)
- 예: `{ $match: { status: "ACTIVE" } }`

2) `$group`
- 그룹화 및 집계
- 예: `{ $group: { _id: "$category", total: { $sum: 1 } } }`

3) `$project`
- 출력 필드/계산 필드 정의
- 예: `{ $project: { category: "$_id", total: 1, _id: 0 } }`

4) `$sort`, `$limit`, `$skip`
- 정렬/페이징

5) `$lookup`
- 다른 컬렉션과 조인 유사 처리

**팁**
- `$match` + 적절한 인덱스가 있으면 성능이 크게 좋아집니다.
- `$group` 이후에는 인덱스가 의미 없을 수 있어 앞단에서 최대한 줄이는 것이 중요합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000010';

UPDATE questions
SET explanation =
'**Map-Reduce 사용 시점**
- 과거에는 복잡한 집계를 Map-Reduce로 처리했지만, 현재는 대부분 **Aggregation**이 더 빠르고 편리해서 Map-Reduce 사용이 줄었습니다.

**그래도 고려될 수 있는 경우**
1) 매우 특수한 사용자 정의 집계 로직이 필요
2) 기존 시스템에 Map-Reduce 기반 로직이 이미 존재해 유지보수해야 함
3) 집계 파이프라인으로 표현하기 어려운 커스텀 처리

**실무 권장**
- 신규 개발에서는 대개 **Aggregation 우선**으로 접근하고, 정말 필요한 경우에만 Map-Reduce를 검토하는 편이 일반적입니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000011';

UPDATE questions
SET explanation =
'**트랜잭션 지원 여부**
- MongoDB는 **트랜잭션을 지원**합니다.
- 기본적으로 **단일 문서의 변경은 원자적**입니다.
- 그 외에도 여러 문서/컬렉션에 걸친 **멀티-도큐먼트 트랜잭션**을 지원합니다(Replica Set 및 샤딩 환경에서도 가능).

**범위/특징**
1) 세션(Session) 기반으로 시작/커밋/롤백을 수행
2) ACID 성격을 제공하지만, 분산 환경에서는 비용이 증가할 수 있음
3) 장시간 트랜잭션은 락/리소스 사용으로 성능에 영향을 줄 수 있음

**설계 팁**
- 가능하면 “한 문서에 필요한 데이터를 묶어” 단일 문서 원자성으로 해결하고,
- 정말 필요한 경우에만 멀티 문서 트랜잭션을 사용하면 운영이 단순해집니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000012';

UPDATE questions
SET explanation =
'**Replica Set이란**
- MongoDB의 **고가용성(HA)** 구성으로, 동일 데이터를 여러 노드에 복제해 두는 구조입니다.
- 일반적으로 **Primary 1대 + Secondary 여러 대**로 구성됩니다.

**왜 쓰나**
1) 장애 대응: Primary 장애 시 자동으로 새 Primary를 선출(Failover)
2) 읽기 확장: Secondary로 읽기 분산 가능(읽기 선호도 설정)
3) 데이터 안전성: 복제본을 통해 데이터 유실 위험 감소(Write Concern과 함께)

**주의**
- 복제는 비동기/동기 설정에 따라 지연이 생길 수 있어, 읽기 일관성(Read Concern/Read Preference)을 설계해야 합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000013';

UPDATE questions
SET explanation =
'**Primary**
- 모든 **쓰기(Insert/Update/Delete)**의 기본 대상 노드입니다.
- 클라이언트 요청을 처리하고, 변경 내용을 Oplog 형태로 기록해 Secondary에 전파합니다.

**Secondary**
- Primary의 변경 사항을 복제해 동일 데이터를 유지하는 노드입니다.
- 기본적으로 읽기 요청은 Primary가 담당하지만, 설정에 따라 Secondary로 읽기 분산이 가능합니다.
- Secondary 중 하나는 필요 시 선거(election)를 통해 Primary가 될 수 있습니다.

**정리**
- Primary = 쓰기 중심(기본)
- Secondary = 복제/읽기 분산/장애 대비'
WHERE id = 'b0000000-0000-0000-0012-000000000014';

UPDATE questions
SET explanation =
'**Automatic Failover 동작**
1) Replica Set 내부에서 노드들이 서로 상태를 감시(heartbeat)
2) Primary가 다운/네트워크 단절로 판단되면 **선거(election)**를 수행
3) Secondary 중 하나가 새 **Primary**로 승격
4) 클라이언트는 드라이버가 Replica Set 정보를 기반으로 새 Primary에 재연결(재시도/재선택)

**주의**
- 장애 전환 동안 짧은 시간 쓰기 실패/재시도가 발생할 수 있습니다.
- Write Concern이 약하면(예: acknowledged만) 장애 순간에 최신 쓰기가 유실될 가능성이 있으므로, 요구되는 내구성에 맞게 설정해야 합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000015';

UPDATE questions
SET explanation =
'**Sharding이란**
- 데이터를 여러 서버(샤드)로 **분산 저장**해 용량/처리량을 수평 확장하는 방식입니다.

**MongoDB에서의 구현 개요**
1) **Shard**: 데이터를 나눠 갖는 저장소(보통 Replica Set)
2) **Config Server**: 샤딩 메타데이터 관리(청크 범위, 라우팅 정보)
3) **mongos**: 클라이언트 요청을 받아 샤드로 라우팅

**핵심 포인트**
- 데이터 분산 기준은 **Shard Key**이며, 샤드 키에 따라 데이터가 청크 단위로 분배/이동됩니다.

**주의**
- 샤딩을 도입하면 운영 복잡도가 증가하므로, 데이터 규모/트래픽이 충분히 커질 때 설계/검증 후 도입하는 편이 안전합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000016';

UPDATE questions
SET explanation =
'**Shard Key 선택 기준(매우 중요)**
1) **높은 카디널리티(다양한 값)**
- 값이 다양해야 여러 샤드로 골고루 분산됩니다.

2) **핫스팟 방지**
- 단조 증가 값(시간, 시퀀스)만 쓰면 특정 범위에 쓰기가 몰려 핫스팟이 생길 수 있습니다.
- 필요하면 해시 샤딩(hash shard key) 등을 고려합니다.

3) **쿼리 패턴과 일치**
- 자주 사용하는 조회 조건에 샤드 키가 포함되면 라우팅이 효율적(타겟팅 쿼리)
- 샤드 키가 조건에 없으면 모든 샤드를 뒤지는 브로드캐스트 쿼리 가능성이 커집니다.

4) **변경 불가/변경 비용**
- 샤드 키는 바꾸기 매우 어렵거나 비용이 큽니다. 초기 설계가 중요합니다.

**요약**
- “분산(균형)”과 “조회 패턴(타겟팅)”을 동시에 만족시키는 키를 고르는 것이 핵심입니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000017';

UPDATE questions
SET explanation =
'**직접 조인이 약한 이유(배경)**
- MongoDB는 기본 철학이 “관계형 조인으로 모델링”이 아니라, **문서 중첩(embedding)**과 **분산 확장**에 최적화된 모델입니다.
- 특히 샤딩/분산 환경에서 조인을 일반화하면, 네트워크 이동/데이터 재분배가 많이 발생해 비용이 커질 수 있습니다.

**대신 사용하는 방식**
1) **Embedding**
- 함께 조회되는 데이터를 한 문서에 넣어 조인을 피함(읽기 최적화)

2) **Reference + 애플리케이션 조합**
- 참조 키를 저장하고, 애플리케이션에서 두 번 조회하거나 캐시/배치로 보완

3) `$lookup`
- 집계 파이프라인에서 조인 유사 기능 제공(가능하나 비용/제약 고려)

**정리**
- MongoDB에서도 조인 비슷한 것은 가능하지만, 설계는 “조인 최소화” 방향이 일반적입니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000018';

UPDATE questions
SET explanation =
'**Embedding(임베디드 문서)**
- 관련 데이터를 한 문서에 **중첩**해서 저장합니다.
- 언제 좋나:
  1) 항상 함께 조회/갱신되는 관계(예: 주문 + 주문아이템)
  2) 읽기 성능이 중요하고 조인을 피하고 싶을 때
- 장점: 한 번의 조회로 끝, 단일 문서 원자성 활용
- 단점: 문서 크기 증가, 중복/갱신 비용 증가 가능

**Reference(DBRef/참조)**
- 다른 컬렉션의 문서를 **참조 키**로 연결합니다.
- 언제 좋나:
  1) 1:N 관계에서 N이 매우 커서 embedding이 위험할 때
  2) 여러 곳에서 공유되는 엔티티(예: 상품, 사용자 프로필)
- 장점: 중복 감소, 문서 크기 관리 용이
- 단점: 추가 조회 필요, 일관성/성능 설계 필요

**실무 결론**
- “같이 쓰는 건 embedding”, “공유/대규모는 reference”가 기본 방향이고,
- 실제 쿼리 패턴/문서 크기/갱신 빈도에 따라 결정합니다.'
WHERE id = 'b0000000-0000-0000-0012-000000000019';

UPDATE questions
SET explanation =
'**GridFS란**
- MongoDB에서 큰 파일(이미지/동영상/로그 등)을 저장하기 위한 규격/기능입니다.
- 단일 BSON 문서 크기 제한이 있으므로, 큰 파일을 **청크(chunk)**로 잘라 여러 문서로 저장하고 메타데이터를 함께 관리합니다.

**언제 쓰나**
1) 파일 저장용 전용 스토리지(S3 등)를 쓰기 어렵거나
2) 파일과 메타데이터를 MongoDB에서 함께 관리하고 싶을 때
3) 스트리밍/부분 읽기 같은 기능이 필요할 때

**주의**
- 대규모 파일 스토리지에는 오브젝트 스토리지(S3 등)가 더 적합한 경우가 많아, 요구사항에 따라 선택하는 것이 좋습니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000001A';

UPDATE questions
SET explanation =
'**성능 비교 관점(일반론)**
- MySQL(RDBMS)은 조인/정규화/트랜잭션 중심의 일관된 처리에 강합니다.
- MongoDB는 문서 기반으로 읽기 패턴을 잘 맞추면(embedding + 인덱스) 특정 조회에서 매우 빠를 수 있고, 수평 확장도 상대적으로 수월합니다.

**보통 MongoDB가 유리해질 수 있는 경우**
1) 스키마 변화가 잦고 유연성이 필요
2) 읽기 중심이며, 함께 조회되는 데이터를 한 문서로 묶기 쉬움
3) 데이터가 커지고 노드 추가로 확장해야 함(샤딩)

**MySQL이 유리한 경우**
1) 복잡한 조인/정규화가 핵심
2) 강한 트랜잭션/일관성이 필수
3) 보고서/통계가 SQL 조합으로 잘 해결됨

**결론**
- “어느 쪽이 더 빠르다”가 아니라, **데이터 모델/쿼리 패턴/트랜잭션 요구**에 따라 유불리가 갈립니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000001B';

UPDATE questions
SET explanation =
'**백업/복원 기본 도구**
1) `mongodump` / `mongorestore`
- BSON 기반 덤프/복원 도구(컬렉션 단위/쿼리 기반 덤프 가능)

2) 파일시스템 스냅샷
- 스토리지 레벨 스냅샷으로 빠르게 백업(운영 환경에서 사용)
- 일관성 보장을 위해 보통 fsync lock 등 전략을 함께 고려

3) 클라우드/매니지드 서비스 백업
- Atlas 등에서는 스냅샷/포인트 인 타임 복구(PITR) 같은 기능을 제공합니다.

**주의**
- Replica Set/샤딩 환경에서는 백업 전략(어떤 노드에서 덤프할지, 일관성/부하)을 같이 설계해야 합니다.
- 백업은 “복원 테스트”까지 포함해야 의미가 있습니다.'
WHERE id = 'b0000000-0000-0000-0012-00000000001C';

UPDATE questions
SET explanation =
'**mongodump**
- MongoDB의 데이터를 **BSON** 형태로 덤프하는 백업 도구입니다.
- 복원은 `mongorestore`로 수행합니다.
- 장점: 인덱스/타입 정보 포함, 복원에 유리

**mongoexport**
- 데이터를 **JSON/CSV** 같은 텍스트 포맷으로 내보내는 도구입니다.
- 주로 분석/외부 시스템 전달/간단한 데이터 추출에 사용합니다.
- 장점: 사람이 읽기 쉽고 다른 시스템과 호환이 쉬움
- 단점: BSON 타입/인덱스 정보 등 DB 고유 정보가 손실될 수 있음(복원용 백업에는 부적합)

**정리**
- “백업/복원 목적”이면 mongodump/mongorestore
- “데이터 추출/교환 목적”이면 mongoexport/mongoimport'
WHERE id = 'b0000000-0000-0000-0012-00000000001D';