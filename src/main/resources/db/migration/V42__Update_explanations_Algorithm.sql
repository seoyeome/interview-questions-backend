-- Algorithm 면접 질문 답변 추가 (explanation 컬럼 업데이트)
-- 전제: questions 테이블에 explanation 컬럼(TEXT/VARCHAR)이 존재한다고 가정합니다.

UPDATE questions
SET explanation =
'**개념**
- “평균적으로 빠르다”는 보통 **평균 시간복잡도**와 **상수 계수**, 그리고 **실제 구현 최적화**까지 포함해 판단합니다.
- 이론적으로 비교 기반 정렬의 평균 시간복잡도 하한은 O(n log n)이며, 대표적으로 **퀵 정렬(Quick Sort)**, **병합 정렬(Merge Sort)**, **힙 정렬(Heap Sort)** 등이 여기에 속합니다.

**정답 포인트(현업/면접 관점)**
- 평균적으로 가장 빠른 것으로 흔히 **퀵 정렬**을 말합니다(평균 O(n log n), 캐시 친화적, in-place 가능).
- 다만 실무의 표준 라이브러리 정렬은 “퀵 정렬 단독”이 아니라 최악 케이스를 피하기 위해
  - Introsort(퀵 + 힙) 또는
  - Timsort(삽입 + 병합)
  같은 하이브리드가 많습니다.

**예시**
- 랜덤 데이터에서 퀵 정렬은 평균적으로 매우 빠르게 동작합니다.
- 정렬이 거의 되어 있는 배열에서는 Timsort가 더 유리할 수 있습니다.

**비교/응용**
- 평균 속도만 보면 퀵 정렬이 강하지만, 안정성(stable), 추가 메모리 사용, 최악 케이스까지 고려해 선택합니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000000';

UPDATE questions
SET explanation =
'**개념**
- **버블 정렬(Bubble Sort)**은 인접한 두 원소를 비교해 **순서가 잘못되면 교환(swap)**을 반복하여,
  큰 값(또는 작은 값)이 한 번의 패스마다 끝으로 “거품처럼” 이동하는 정렬입니다.

**동작 방식**
1) 배열의 앞에서부터 인접 원소 (a[i], a[i+1])를 비교
2) 정렬 기준에 어긋나면 swap
3) 한 바퀴(패스)를 돌면 가장 큰 값이 맨 뒤로 확정
4) 뒤쪽 확정 구간을 제외하고 다시 1)~3) 반복

**예시**
- [5, 1, 4, 2]
  - 1패스: (5,1) swap → [1,5,4,2], (5,4) swap → [1,4,5,2], (5,2) swap → [1,4,2,5]
  - 맨 뒤 5 확정, 나머지 구간 반복

**비교/응용**
- 시간복잡도: 평균/최악 O(n^2), 최선(이미 정렬 + 조기 종료 최적화) O(n)
- 구현은 쉬우나 대부분의 실무/대규모 데이터에는 비효율적이라 학습용으로 주로 다룹니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000001';

UPDATE questions
SET explanation =
'**개념**
- 퀵 정렬의 최악 시간복잡도 O(n^2)은 **분할이 극단적으로 불균형**할 때 발생합니다.
- 즉, 피벗(pivot)이 매번 최솟값/최댓값에 가깝게 잡혀 한쪽 부분배열 크기가 n-1, 다른 쪽이 0에 가까워지는 경우입니다.

**대표 입력 케이스**
- 피벗을 “첫 원소/마지막 원소”로 고정하는 구현에서
  - 이미 오름차순 정렬된 배열
  - 이미 내림차순 정렬된 배열
  - 모든 값이 같거나 중복이 매우 많은 배열(파티셔닝 구현이 단순하면)  
  등이 최악 케이스를 유발할 수 있습니다.

**예시**
- pivot = 첫 원소, 입력 = [1,2,3,4,5]
  - pivot 1 기준으로 분할 → 왼쪽 0, 오른쪽 4
  - 반복되며 재귀 깊이가 커져 O(n^2)

**비교/응용(회피 전략)**
- 랜덤 피벗, median-of-three, 3-way partition(중복 대응), Introsort(재귀 깊이 커지면 힙 정렬로 전환)로 최악 케이스를 방지합니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000002';

UPDATE questions
SET explanation =
'**개념**
- 여러 정렬 알고리즘을 비교할 때는 “빅오”만 보지 않고, 요구사항과 데이터 특성을 기준으로 평가해야 합니다.

**중요 기준**
1) **시간복잡도**: 평균/최악/최선 케이스
2) **공간복잡도**: 추가 메모리 사용 여부(in-place 여부)
3) **안정성(Stable Sort)**: 같은 키의 상대적 순서 보존 여부
4) **데이터 특성**: 거의 정렬됨, 중복 많음, 랜덤, 범위 제한(정수 범위 작음) 등
5) **상수 계수/캐시 친화성**: 실제 성능에 크게 영향
6) **병렬화/외부 정렬 가능성**: 대용량 파일(디스크) 정렬 등
7) **구현 난이도/버그 위험/라이브러리 지원**

**예시**
- 로그를 (timestamp, sequence)로 정렬해야 “같은 timestamp”의 원래 순서가 중요하면 stable 정렬을 선택합니다.
- 메모리가 매우 제한된 환경이면 in-place 정렬을 우선 고려합니다.

**비교/응용**
- 알고리즘 선택은 ‘문제 제약’과 ‘데이터 분포’를 먼저 보고 결정하는 것이 면접에서 좋은 답변 흐름입니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000003';

UPDATE questions
SET explanation =
'**개념**
- **백트래킹(Backtracking)**은 해를 구성하는 과정에서 “부분 해(partial solution)”를 확장해가다가,
  더 진행해도 정답이 될 수 없으면 그 지점에서 **되돌아가(prune)** 다른 선택지를 탐색하는 **완전탐색(DFS) 기반 기법**입니다.

**핵심 특징**
- 모든 경우를 보되, 조건 위반/불가능이 확정되면 가지치기하여 탐색 공간을 줄입니다.
- 보통 재귀/스택을 이용한 DFS 형태로 구현됩니다.

**예시**
- N-Queen: 각 행에 퀸을 하나씩 두면서, 같은 열/대각선 충돌이 나면 더 내려가지 않고 되돌아감
- 순열/조합 생성: 이미 사용한 원소는 제외하고 다음 깊이로 진행

**비교/응용**
- 백트래킹은 최악에는 여전히 지수 시간일 수 있으나,
  가지치기 조건이 강할수록 실제 수행 시간이 크게 줄어듭니다.
- 제약 충족 문제(CSP), 퍼즐/스도쿠, 조합 최적화 문제의 기본 접근으로 자주 쓰입니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000004';

UPDATE questions
SET explanation =
'**개념**
- **분기 한정(Branch and Bound)**는 최적화 문제에서 탐색 트리를 확장(Branch)하되,
  현재까지 구한 최적해보다 더 좋아질 가능성이 없는 분기는 **상한/하한(Bound)**을 이용해 잘라내는 기법입니다.
- “백트래킹 + 최적해 보장 + 경계값으로 강한 가지치기”라고 보면 이해가 빠릅니다.

**동작 흐름**
1) 부분해를 노드로 하는 탐색 트리를 만든다(Branch)
2) 각 노드에서 목적함수의 **최선 가능값(하한/상한)**을 계산한다(Bound)
3) 그 bound가 이미 가진 최적해보다 나쁘면 그 분기는 탐색하지 않는다(Prune)

**예시**
- 0/1 배낭(Knapsack): 남은 용량에서 얻을 수 있는 최대 가치(상한)를 계산해,
  현재 최적 가치보다 상한이 낮으면 그 분기는 버림
- TSP(외판원): 남은 경로 비용의 하한을 이용해 비유망 경로를 제거

**비교/응용**
- 백트래킹이 “조건 위반” 중심의 pruning이라면,
  BnB는 “최적화 관점의 bound”로 pruning을 수행합니다.
- bound 계산이 정확할수록 pruning이 강해지지만, 계산 비용도 함께 늘 수 있습니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000005';

UPDATE questions
SET explanation =
'**개념**
- **다이나믹 프로그래밍(DP)**은 큰 문제를 작은 부분 문제로 나누고,
  부분 문제의 해를 저장해 재사용하여 중복 계산을 제거하는 기법입니다.

**피보나치 DP(바텀업)**
- 점화식: F(n) = F(n-1) + F(n-2), F(0)=0, F(1)=1
- 바텀업은 작은 값부터 차례대로 채웁니다.

**예시(아이디어)**
1) dp[0]=0, dp[1]=1
2) i=2..n까지 dp[i]=dp[i-1]+dp[i-2]
3) dp[n] 반환

**비교/응용**
- 재귀(단순) 피보나치는 중복 호출로 O(2^n)
- DP는 O(n)으로 줄어듭니다.
- 추가 최적화로 dp 배열 없이 최근 두 값만 들고 O(1) 공간으로도 가능합니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000006';

UPDATE questions
SET explanation =
'**개념**
- **메모이제이션(Memoization)**은 “한 번 계산한 결과를 캐시에 저장해, 같은 입력이 다시 나오면 즉시 반환”하는 기법입니다.
- DP의 구현 방식 중 **탑다운(재귀 + 캐시)**에서 핵심으로 사용됩니다.

**DP에서의 활용**
- 점화식을 재귀로 표현하고, 함수 f(x)를 호출할 때
  1) 이미 계산된 값이면 캐시에서 반환
  2) 아니면 계산 후 캐시에 저장
  형태로 중복 호출을 제거합니다.

**예시**
- 피보나치:
  - f(10)을 계산하려면 f(9), f(8)이 필요
  - f(8)을 여러 번 계산하는 중복이 발생하는데,
    memo[8]에 저장하면 다음부터는 바로 반환

**비교/응용**
- 바텀업(dp 테이블 채우기) vs 탑다운(메모이제이션)
  - 바텀업: 반복문, 실행 흐름이 명확
  - 탑다운: 필요한 부분만 계산(희소 상태 공간에 유리), 구현이 직관적일 수 있음
- 둘 다 본질은 “부분 문제 결과 재사용”입니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000007';

UPDATE questions
SET explanation =
'**개념**
- **탐욕(Greedy) 알고리즘**은 매 단계에서 “지금 당장 최선의 선택”을 하여 전체 최적해를 얻으려는 전략입니다.
- 핵심은 항상 최적이 되는 것이 아니라, **문제가 탐욕 선택 속성**과 **최적 부분 구조**를 만족할 때만 정답이 됩니다.

**예시**
- 거스름돈 문제(동전 단위가 500,100,50,10처럼 정규 체계일 때):
  - 가장 큰 동전부터 최대한 사용 → 동전 개수 최소
- 활동 선택 문제(Activity Selection):
  - 끝나는 시간이 가장 빠른 활동부터 선택

**비교/응용**
- 그리디는 보통 구현이 간단하고 빠르지만,
  적용 가능 조건을 증명(또는 반례 확인)해야 합니다.
- DP는 더 넓은 범위의 최적화 문제를 풀 수 있지만, 상태 설계/메모리 비용이 큽니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000008';

UPDATE questions
SET explanation =
'**개념**
- **분할 정복(Divide and Conquer)**은 문제를 더 작은 하위 문제로 나누고(Divide),
  각각을 해결한 뒤(Conquer),
  결과를 합쳐(Combine) 원래 문제를 푸는 기법입니다.

**거꾸로 생각한다는 의미(맥락)**
- “전체를 직접 풀기 어렵다면, 더 작은 문제들의 해를 조합해서 전체 해를 만든다”는 관점 전환을 말하는 경우가 많습니다.
- 즉, 직접적인 한 번의 절차보다 “쪼개서 해결 후 합치기”로 접근하는 사고 방식입니다.

**예시**
- 병합 정렬: 반으로 나누어 정렬(재귀) 후, 두 정렬된 배열을 병합
- 퀵 정렬: 피벗 기준으로 분할 후 각 부분을 정렬(단, 병합 단계가 사실상 없음)

**비교/응용**
- 분할 정복은 재귀 깊이/스택 사용을 고려해야 하며,
  병합 정렬처럼 추가 메모리가 필요한 경우도 있습니다.
- 병렬화가 가능한 구조라 대규모 처리에서 강점이 있습니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000009';

UPDATE questions
SET explanation =
'**개념**
- **해싱(Hashing)**은 키(key)를 해시 함수(hash function)로 변환해 **배열 인덱스(버킷)**로 매핑하고,
  이를 이용해 평균적으로 빠른 탐색/삽입/삭제를 수행하는 기법입니다.
- 목표: 평균 O(1)에 가까운 성능(충돌이 적고 리사이징이 적절할 때)

**충돌(Collision)**
- 서로 다른 키가 같은 해시값/버킷으로 매핑되는 현상입니다.
- 충돌을 완전히 없앨 수는 없어서, 해결 전략이 필요합니다.

**충돌 해결 방법**
1) **체이닝(Chaining)**: 버킷마다 연결 리스트/트리 구조로 여러 항목 저장
2) **개방 주소법(Open Addressing)**: 충돌 시 다른 빈 칸을 찾아 저장
   - 선형 탐사(Linear Probing)
   - 이차 탐사(Quadratic Probing)
   - 이중 해싱(Double Hashing)
3) **리사이징(Resizing/Rehashing)**: 적재율(load factor)이 높아지면 테이블 크기를 키워 충돌률을 낮춤

**비교/응용**
- 체이닝은 구현이 단순하고 삭제가 비교적 쉬움
- 개방 주소법은 캐시 친화적일 수 있으나 클러스터링 문제가 생길 수 있음
- 실무에서는 라이브러리 구현(체이닝+트리화 등)을 신뢰하는 편이 일반적입니다.'
WHERE id = 'b0000000-0000-0000-0020-00000000000a';

UPDATE questions
SET explanation =
'**개념**
- **HashMap**과 **TreeMap**은 모두 키-값(Map) 자료구조지만, 내부 구현과 보장되는 성질이 다릅니다.

**HashMap**
- 내부: 해시 테이블 기반
- 평균 시간: put/get/remove가 평균 O(1)
- 순서: 키의 정렬/삽입 순서 보장 없음(LinkedHashMap은 삽입 순서 보장)
- 키 요구사항: equals/hashCode 규약 필요

**TreeMap**
- 내부: 균형 이진 탐색 트리(보통 Red-Black Tree)
- 시간: put/get/remove가 O(log n)
- 순서: 키를 **정렬된 순서**로 유지
- 기능: 범위 탐색, 최소/최대 키, lower/higher 같은 네비게이션 API가 강점

**예시**
- 빠른 단건 조회/캐시 키: HashMap
- 정렬된 키 순회, 구간 조회(예: 10~20 사이 키): TreeMap

**비교/응용**
- 정렬/범위 연산이 필요하면 TreeMap이 설계적으로 맞고,
- 단순 조회 성능이 중요하면 HashMap이 보통 더 유리합니다.'
WHERE id = 'b0000000-0000-0000-0020-00000000000b';

UPDATE questions
SET explanation =
'**개념**
- **우선순위 큐(Priority Queue)**는 “가장 우선순위가 높은 원소”를 빠르게 꺼내는 자료구조입니다.
- 보통 min-heap(최솟값 우선) 또는 max-heap(최댓값 우선) 형태로 사용합니다.

**내부 구현**
- 가장 흔한 구현은 **이진 힙(Binary Heap)**입니다.
  - 완전 이진 트리 형태를 배열로 표현
  - 삽입/삭제(최솟값/최댓값 제거): O(log n)
  - 최솟값/최댓값 조회(peek): O(1)

**대안 구현**
- 균형 BST(TreeSet/TreeMap 기반): O(log n)로 유사하지만 상수 계수가 다름
- 피보나치 힙: 이론적으로 decrease-key가 빠르지만 구현 복잡, 실무에서 흔치 않음

**예시**
- 다익스트라(Dijkstra)에서 현재 거리 최솟값 노드를 뽑는 용도로 사용합니다.

**비교/응용**
- “항상 가장 작은/큰 것만 빨리 뽑으면 되는” 상황에 힙 기반 우선순위 큐가 표준 선택입니다.'
WHERE id = 'b0000000-0000-0000-0020-00000000000c';

UPDATE questions
SET explanation =
'**개념**
- **Dijkstra 알고리즘**은 가중치가 있는 그래프에서 한 시작점으로부터 다른 모든 정점까지의 **최단 경로(Shortest Path)**를 구하는 알고리즘입니다.
- 조건: 간선 가중치가 **음수가 없어야** 합니다(0은 가능).

**핵심 아이디어**
- 현재까지 알려진 최단 거리 중 가장 작은 정점을 선택(우선순위 큐)
- 그 정점을 통해 인접 정점의 거리를 갱신(Relaxation)
- 이를 반복하면 최단 거리가 확정됩니다.

**예시**
- 지도 길찾기: 도로 길이가 모두 양수일 때 한 지점에서 다른 지점까지의 최단 거리
- 네트워크 라우팅(링크 비용 양수)

**비교/응용**
- 음수 가중치가 있으면 Bellman-Ford를 고려해야 합니다.
- 단일 출발점 최단 경로에 특히 강하고, 우선순위 큐를 쓰면 보통 O((V+E) log V)로 구현합니다.'
WHERE id = 'b0000000-0000-0000-0020-00000000000d';

UPDATE questions
SET explanation =
'**개념**
- 최단 경로(Shortest Path)는 그래프에서 두 정점(또는 하나의 시작점에서 여러 정점) 사이의 최소 비용 경로를 구하는 문제입니다.
- 문제 조건(음수 간선, 모든 쌍, DAG 여부 등)에 따라 알고리즘 선택이 달라집니다.

**대표 알고리즘**
1) **Dijkstra**
   - 단일 출발점, 음수 간선 없음
2) **Bellman-Ford**
   - 단일 출발점, 음수 간선 허용(음수 사이클 탐지 가능)
3) **Floyd-Warshall**
   - 모든 쌍 최단 경로(All-Pairs), O(V^3)
4) **Johnson**
   - 모든 쌍, 음수 간선 가능(음수 사이클은 불가), Dijkstra를 여러 번 + 재가중치
5) **DAG 최단 경로**
   - DAG에서는 위상 정렬 후 선형 시간에 해결 가능
6) **A*** (A-star)
   - 휴리스틱을 이용한 경로 탐색(최단 경로를 빠르게 찾는 실용적 방법, 조건 충족 시 최적 보장)

**예시/선택 기준**
- 정점 수가 작고 모든 쌍이 필요하면 Floyd-Warshall
- 음수 간선이 있으면 Bellman-Ford 또는 Johnson
- 대규모 도로망 같은 실용 문제는 A*를 많이 씁니다.'
WHERE id = 'b0000000-0000-0000-0020-00000000000e';

UPDATE questions
SET explanation =
'**개념**
- **NP-완전(NP-Complete)**은
  1) 문제의 해가 주어졌을 때 다항 시간에 검증 가능한 문제(NP)에 속하고,
  2) NP의 모든 문제를 다항 시간에 이 문제로 환원(reduction)할 수 있는(NP-hard) 문제
  를 말합니다.
- 즉, NP 안에서 “가장 어렵다고 알려진 축”의 결정 문제들입니다.

**의미**
- 어떤 NP-완전 문제를 다항 시간에 풀 수 있다면, NP의 모든 문제를 다항 시간에 풀 수 있게 됩니다(P=NP로 이어짐).
- 아직 P=NP는 미해결입니다.

**예시(결정 문제 형태)**
- SAT(부울 만족 가능성)
- 3-SAT
- 정점 커버(Vertex Cover)
- 해밀토니안 경로/사이클(존재 여부)
- 부분집합 합(Subset Sum, 목표 합 존재 여부)

**비교/응용**
- 실무에서는 정확해(최적해) 대신 근사 알고리즘, 휴리스틱, 제한 조건 활용(입력 크기 작음 등)으로 접근하는 경우가 많습니다.'
WHERE id = 'b0000000-0000-0000-0020-00000000000f';

UPDATE questions
SET explanation =
'**개념**
- **NP-하드(NP-hard)**: NP의 모든 문제를 이 문제로 다항 시간에 환원할 수 있는 문제(적어도 NP만큼 어렵다).
  - NP에 속할 필요는 없습니다(해 검증이 다항 시간인지 보장하지 않음).
- **NP-완전(NP-complete)**: NP-hard 이면서 동시에 NP에 속하는 문제.

**정리(핵심 차이)**
- NP-완전 ⊂ NP-하드 (일반적으로)
- NP-하드는 “결정 문제가 아닐 수도” 있고, “검증이 다항 시간일 필요도” 없습니다.

**예시**
- NP-완전: 3-SAT, Vertex Cover(결정형)
- NP-하드: TSP(최단 경로 길이 자체를 최소화하는 최적화 버전)  
  - 최적화 버전은 보통 결정 문제(NP)에 바로 속한다고 말하기 어렵지만, NP-하드로 분류됩니다.
  - 대신 “TSP의 결정 버전(거리 ≤ K인가?)”은 NP-완전으로 알려져 있습니다.

**응용**
- 면접에서는 “결정 버전/최적화 버전”을 구분해서 설명하면 이해도가 높게 평가됩니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000010';

UPDATE questions
SET explanation =
'**개념**
- **P vs NP**는 “정답을 빨리 찾을 수 있는 문제(P)와, 정답을 빨리 검증할 수 있는 문제(NP)가 같은가?”를 묻는 계산복잡도 이론의 핵심 문제입니다.
- P: 다항 시간에 풀 수 있는 결정 문제 집합
- NP: 해가 주어졌을 때 다항 시간에 검증 가능한 결정 문제 집합

**핵심 질문**
- P = NP 인가?
  - 만약 같다면: NP-완전 문제도 다항 시간에 풀 수 있게 됩니다.
  - 만약 다르다면: NP-완전 문제는 (일반적으로) 다항 시간 해법이 없다고 기대합니다.

**왜 중요한가**
- 암호학, 최적화, 스케줄링, 인공지능, 조합 문제 전반에 영향이 큽니다.
- 현대 암호의 안전성 가정 중 일부는 “특정 문제가 빠르게 풀리기 어렵다”는 믿음과 연결됩니다.

**예시**
- SAT는 NP-완전이면서 NP에 속합니다.
- SAT를 다항 시간에 푸는 알고리즘이 발견되면, P=NP에 큰 진전이 됩니다.

**비교/응용**
- 실무에서는 P vs NP가 결론 나기 전에도, 근사/휴리스틱/제약 완화/입력 크기 제한을 통해 현실적인 해법을 설계합니다.'
WHERE id = 'b0000000-0000-0000-0020-000000000011';